Программа принимает аргументами названия двух файлов из консоли, и копирует один файл в другой. 

The program takes as arguments the names of two files from the console, and copies one file to another.
